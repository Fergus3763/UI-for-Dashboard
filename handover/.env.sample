# /handover/supabase/.env.sample
# Copy this to .env locally (DO NOT COMMIT .env) and fill in your project values.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Environment Sample (Hub#2 → Hub#3)

This file defines all required environment variables for local and CI environments.  
All values should mirror the live production configuration in Netlify and Supabase.

---

## 🔧 Core Variables

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NETLIFY_SITE_ID=
NODE_VERSION=18


> ⚠️ **Do not commit secrets or real values.**  
> Only variable *names* belong in this file.

---

## 📦 Additional Optional Vars

These may be used depending on Hub feature scope or integration status:
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
GOOGLE_API_KEY=
FIREBASE_CONFIG=

---

## ✅ Validation Steps
1. Ensure all required keys exist both in:
   - Local `.env` files (frontend + backend)
   - Netlify’s Environment Variables dashboard
   - Supabase Project Settings → API tab
2. Confirm that `SUPABASE_SERVICE_ROLE_KEY` corresponds to the **service role**, not the anon key.
3. Run a local build:
   ```bash
   npm run build
