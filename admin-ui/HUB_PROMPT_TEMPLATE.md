# HUB Prompt Template (Admin UI pointer)

> ⚠️ This file is a redirect.

The canonical HUB Prompt template lives in:

- `handover/HUB_PROMPT.md`

That document defines how each new HUB or SPOKE should be initialized:
purpose, mission format, deliverables, and communication standards.

Do **not** maintain this version; edit only the `/handover` template.
